apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth

spec:
  type: NodePort

  selector:
    app: auth

  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30007